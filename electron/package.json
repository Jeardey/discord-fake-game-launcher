{
  "name": "discord-fake-game-launcher-ui",
  "private": true,
  "version": "0.1.0",
  "description": "Discord Fake Game Launcher app",
  "author": {
    "name": "Jeardey",
    "email": "noreply@users.noreply.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Jeardey/discord-fake-game-launcher.git"
  },
  "main": "src/main.js",
  "type": "commonjs",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "sync:gamelist": "node ./scripts/sync-gamelist.js",
    "build:dummy": "dotnet publish ../src/DummyGame/DummyGame.csproj -c Release -f net8.0-windows -r win-x64 --self-contained true -p:PublishSingleFile=true",
    "build:dummy:win": "dotnet publish ../src/DummyGame/DummyGame.csproj -c Release -f net8.0-windows -r win-x64 --self-contained true -p:PublishSingleFile=true",
    "prepare:dummy": "node ./scripts/prepare-dummygame.js",
    "prepare:dummy:win": "node ./scripts/prepare-dummygame.js --platform=win32",
    "build:launcher": "dotnet build ../src/Launcher/Launcher.csproj -c Release",
    "dist": "electron-builder --publish never",
    "dist:win": "electron-builder --win nsis --publish never",
    "dist:portable": "electron-builder --win portable --publish never",
    "dist:publish": "npm run dist:publish:win",
    "dist:publish:win": "electron-builder --win nsis --publish always"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^24.13.3"
  },
  "dependencies": {
    "electron-updater": "^6.3.0"
  },
  "build": {
    "appId": "com.jeardey.discordfakegamelauncher",
    "productName": "Discord Fake Game Launcher",
    "files": [
      "src/**"
    ],
    "asar": true,
    "directories": {
      "output": "dist"
    },
    "extraResources": [
      {
        "from": "build-resources/dummygame",
        "to": "dummygame",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "artifactName": "${productName} Setup.${ext}",
      "target": [
        "nsis"
      ]
    },
    "publish": [
      {
        "provider": "github",
        "owner": "Jeardey",
        "repo": "discord-fake-game-launcher"
      }
    ],
    "nsis": {
      "oneClick": true,
      "perMachine": true,
      "allowElevation": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Discord Fake Game Launcher"
    }
  }
}
